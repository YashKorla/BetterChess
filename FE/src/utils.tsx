
export const ternaryOperator = (condition:boolean,element1:any,element2:any)=>{
    return condition ? element1 : element2
}

export const andOperator = (condition:boolean,element:any)=>{
    return condition && element
}