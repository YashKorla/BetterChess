import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#171719",
            light: "#222226",
        },
        secondary: {
            main: "#222226",
            light: "#171719",
        }
    }
});

export default theme;